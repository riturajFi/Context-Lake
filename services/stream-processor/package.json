{
  "name": "@context-lake/stream-processor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "lint": "eslint src --ext .ts",
    "test": "node --import tsx --test test/*.test.ts",
    "typecheck": "tsc -b --pretty false",
    "build": "tsc -b"
  },
  "dependencies": {
    "@context-lake/shared-config": "workspace:*",
    "@context-lake/shared-db": "workspace:*",
    "@context-lake/shared-events": "workspace:*",
    "@context-lake/shared-http": "workspace:*",
    "@context-lake/shared-logging": "workspace:*",
    "@context-lake/shared-observability": "workspace:*",
    "kafkajs": "^2.2.4",
    "pg": "^8.14.1"
  },
  "devDependencies": {
    "pg-mem": "^3.0.5"
  }
}
