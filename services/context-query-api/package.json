{
  "name": "@context-lake/context-query-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "lint": "eslint src --ext .ts",
    "test": "node --test",
    "typecheck": "tsc -b --pretty false",
    "build": "tsc -b"
  },
  "dependencies": {
    "@context-lake/shared-config": "workspace:*",
    "@context-lake/shared-http": "workspace:*",
    "@context-lake/shared-logging": "workspace:*",
    "@context-lake/shared-types": "workspace:*",
    "fastify": "^5.2.1"
  }
}
