{
  "name": "@context-lake/shared-db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsc -b --watch --preserveWatchOutput",
    "lint": "eslint src test --ext .ts",
    "test": "node --import tsx --test test/*.test.ts",
    "typecheck": "tsc -b --pretty false",
    "build": "tsc -b"
  },
  "dependencies": {
    "@context-lake/shared-events": "workspace:*",
    "@context-lake/shared-types": "workspace:*",
    "dotenv": "^16.4.7",
    "pg": "^8.14.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "pg-mem": "^3.0.5"
  }
}
