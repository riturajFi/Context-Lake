{
  "name": "@context-lake/shared-config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "dev": "tsc -b --watch --preserveWatchOutput",
    "lint": "eslint src --ext .ts",
    "test": "node --test",
    "typecheck": "tsc -b --pretty false",
    "build": "tsc -b"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "ioredis": "^5.6.1",
    "kafkajs": "^2.2.4",
    "minio": "^8.0.5",
    "pg": "^8.14.1",
    "zod": "^3.24.2"
  }
}
